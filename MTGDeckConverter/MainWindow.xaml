<Window 
    x:Class="MTGDeckConverter.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ignore="http://www.ignore.com"
    mc:Ignorable="d ignore"
    Height="768"
    Width="1024"
    WindowState="Maximized"
    FontSize="14px"
    x:Name="mainWindow"
    Title="{Binding ElementName=mainWindow, Path=WindowTitle}"
    DataContext="{Binding Main, Source={StaticResource Locator}}"
    xmlns:view="clr-namespace:MTGDeckConverter.View"
    >
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Skins/MainSkin.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <view:ImportDeckWizardView DataContext="{Binding Path=ImportDeckWizardVM}" />
    
</Window>
